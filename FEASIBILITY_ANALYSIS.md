# plan.md 실현가능성 분석 보고서

> 현재 코드베이스와 plan.md 개발 계획의 충돌 분석 및 실현가능성 평가

**분석 일자**: 2024-12-04
**분석 대상**: plan.md 3단계 개발 계획 (14주)
**현재 코드**: 약 13,162 라인 (서버 7개 파일, 클라이언트 26개 컴포넌트)

---

## 📊 전체 평가 요약

| 구분 | 평가 | 실현가능성 | 예상 공수 | 리스크 |
|------|------|-----------|----------|--------|
| **Phase 1** | ⭐⭐⭐⭐⭐ | 95% | 2주 | 낮음 |
| **Phase 2** | ⭐⭐⭐⭐ | 80% | 4주 | 중간 |
| **Phase 3** | ⭐⭐⭐ | 60% | 8주+ | 높음 |
| **전체** | ⭐⭐⭐⭐ | 78% | 14주+ | 중간 |

**결론**: Phase 1은 **매우 실현가능**, Phase 2는 **실현가능**, Phase 3는 **조건부 실현가능**

---

## ✅ 강점: 잘 구축된 기반

### 1. 탄탄한 아키텍처
현재 코드베이스는 매우 잘 설계되어 있습니다:

```
✅ 서버 아키텍처
├── gameManager.ts (517줄) - 게임 로직 완벽 분리
├── types.ts (211줄) - TypeScript 타입 안전성
├── aiAdvisor.ts - Gemini AI 이미 통합
├── aiPlayer.ts - AI 플레이어 구현됨
├── events.ts - 100개 이벤트 데이터
└── Socket.io - 실시간 통신 완성

✅ 클라이언트 구조
├── 26개 컴포넌트 - 모듈화 잘됨
├── Zustand 상태관리 - 확장 용이
├── TailwindCSS - 일관된 디자인
├── Framer Motion - 애니메이션 완성
└── React Router - 라우팅 완비
```

**충돌 없음**: plan.md의 새 기능을 추가하기에 최적의 구조입니다.

---

### 2. 핵심 기능 이미 완성

**게임 시스템** (90% 완성)
- ✅ 멀티플레이어 실시간 동기화
- ✅ 턴제 게임 로직
- ✅ 이벤트 시스템 (100개)
- ✅ 투표 시스템
- ✅ 전투 시스템
- ✅ 외교 시스템 (동맹/적대)
- ✅ AI 플레이어 (3단계 난이도)

**소통 기능** (100% 완성)
- ✅ 실시간 채팅 (팀/공개/외교)
- ✅ AI 어드바이저 (Gemini 2.0 Flash)

**교사 기능** (기본 완성)
- ✅ 대시보드 UI
- ✅ 국가별 현황 표시
- ✅ 채팅 로그 수집
- ✅ 이벤트별 그룹화
- ✅ 정답/오답 추적 (`EventHistory` 타입 존재)

---

## 🟢 Phase 1: 출품 필수 기능 (2주)

### 실현가능성: 95% ⭐⭐⭐⭐⭐

Phase 1은 **거의 문제 없이 구현 가능**합니다.

### 1.1 학습 데이터 수집 시스템

**현재 상태**:
```typescript
// types.ts에 이미 EventHistory 타입 존재!
export interface EventHistory {
  turn: number;
  year: number;
  eventId: string;
  eventTitle: string;
  choiceId: string;
  choiceText: string;
  isHistorical: boolean; // 정답 여부 추적
  timestamp: number;
}

// Team 타입에 이미 포함됨
export interface Team {
  eventHistory: EventHistory[];
}
```

**충돌 없음**:
- ✅ 데이터 구조 이미 존재
- ✅ 정답/오답 추적 이미 구현됨
- ✅ `gameManager.ts`에서 이미 수집 중

**필요한 작업**:
```typescript
// plan.md의 LearningLog 타입 추가만 하면 됨
export interface LearningLog {
  // 기존 EventHistory + 추가 필드
  playerId: string;
  playerName: string;
  logType: 'event_choice' | 'battle' | 'diplomacy' | 'chat' | 'advisor_use';
  // ...
}
```

**예상 공수**: 2-3일 (plan.md 예상 3일과 일치)
**리스크**: 낮음

---

### 1.2 교사용 대시보드 v1 확장

**현재 상태**:
```typescript
// TeacherDashboardPage.tsx - 820줄, 매우 완성도 높음
✅ 이미 구현된 기능:
- 국가별 진도 현황
- 정답률 계산 (completedTurns, correctChoices, incorrectChoices)
- 이벤트 히스토리 표시
- 채팅 로그 (이벤트별 그룹화)
- 실시간 업데이트 (5초 interval)
- API 엔드포인트: /api/teacher/dashboard/:roomCode
```

**충돌 없음**: 기존 대시보드가 너무 잘 만들어져 있어서 **확장만 하면 됨**.

**필요한 작업**:
- 학생별 테이블 추가 (현재는 국가별만)
- 차트 라이브러리 추가 (`react-chartjs-2`)
- CSV 내보내기 버튼

**예상 공수**: 2-3일 (plan.md 예상 3일과 일치)
**리스크**: 낮음

---

### 1.3 교육과정 연계 문서

**충돌 없음**: 코드 작업 아님, 문서 작성만.

**예상 공수**: 1일 (plan.md와 일치)
**리스크**: 없음

---

### 1.4 수업 지도안 작성

**충돌 없음**: 코드 작업 아님.

**참고**:
- 기존 `TutorialPage.tsx`에 튜토리얼 이미 구현됨
- 게임 플로우가 명확해서 지도안 작성 용이

**예상 공수**: 2일 (plan.md와 일치)
**리스크**: 없음

---

### 1.5 즉각적 피드백 시스템

**현재 상태**:
```typescript
// events.ts에 이미 historicalContext 존재
export interface HistoricalEvent {
  historicalContext: string; // 역사적 배경 설명
  choices: Choice[]; // isHistorical 필드로 정답 여부
}

// EventModal.tsx에서 이미 표시 중
```

**충돌 없음**:
- ✅ 데이터 구조 존재
- ✅ UI 컴포넌트 존재
- ✅ 정답/오답 판별 가능

**필요한 작업**:
- 피드백 메시지 100개 작성 (이벤트마다)
- `FeedbackModal` 컴포넌트 추가
- 선택 직후 모달 표시

**예상 공수**: 2일 (plan.md와 일치)
**리스크**: 낮음 (콘텐츠 작성이 주 작업)

---

## 🟡 Phase 2: 경쟁력 강화 (4주)

### 실현가능성: 80% ⭐⭐⭐⭐

Phase 2는 **실현 가능하지만 일부 조정 필요**.

### 2.1 학습 리포트 PDF 생성

**현재 상태**:
- ❌ PDF 생성 기능 없음
- ✅ 데이터는 모두 수집 중 (EventHistory)

**충돌 발견**: plan.md에서 `pdfkit` 사용 제안

**문제점**:
```typescript
// pdfkit은 Node.js 서버에서만 작동
// 브라우저에서 직접 PDF 생성 불가능
```

**해결책**:
```typescript
// 옵션 1: 서버에서 생성 (권장)
app.get('/api/teacher/report/:roomCode/:playerId/pdf', (req, res) => {
  const report = reportGenerator.generateReport(...);
  const pdfBuffer = pdfkit 사용;
  res.send(pdfBuffer);
});

// 옵션 2: 클라이언트에서 생성
// jsPDF 또는 html2pdf.js 사용 (더 간단함)
import html2pdf from 'html2pdf.js';
```

**권장**: 옵션 2 (클라이언트에서 HTML → PDF 변환)가 더 간단하고 빠름.

**예상 공수**: 5-7일 (plan.md는 5일 - 약간 빠듯함)
**리스크**: 중간 (PDF 생성 라이브러리 학습 필요)

---

### 2.2 역사 자료실 구축

**현재 상태**:
- ❌ 역사 자료실 페이지 없음
- ✅ `events.ts`에 100개 이벤트 + historicalContext 존재

**충돌 없음**:
- 새 페이지 추가
- 새 데이터 파일 추가

**필요한 작업**:
```typescript
// 데이터 작성 (콘텐츠 작업)
historicalResources.ts (50개 연표, 30명 인물, 30개 문화유산, 100개 용어)

// 새 페이지 생성
HistoricalResourcesPage.tsx
```

**예상 공수**: 5일 (plan.md와 일치)
**리스크**: 낮음 (주로 콘텐츠 작성)

---

### 2.3 역사 지도 시각화

**현재 상태**:
- ✅ `GameMap.tsx` 존재 (지도 UI)
- ❌ 영토 변화 애니메이션 없음

**충돌 없음**: 기존 GameMap 확장

**문제점**: SVG 지도 데이터 필요
```typescript
// 삼국시대 영토 변화 SVG/GeoJSON 데이터 구하기 어려움
```

**대안**:
```typescript
// 간단한 이미지 기반 지도 + 오버레이
// 또는 Canvas로 그리기
```

**예상 공수**: 3-5일 (plan.md는 4일 - 현실적)
**리스크**: 중간 (지도 데이터 확보가 관건)

---

### 2.4 성취 배지 시스템

**현재 상태**:
- ❌ 배지 시스템 없음

**충돌 없음**: 완전히 새로운 기능

**필요한 작업**:
```typescript
// 서버: achievementManager.ts (새 파일)
// 클라이언트: 배지 UI 컴포넌트
// 데이터: 30개 배지 정의
```

**예상 공수**: 3일 (plan.md와 일치)
**리스크**: 낮음 (비교적 독립적인 기능)

---

### 2.5 오답 노트 기능

**현재 상태**:
- ✅ EventHistory에 `isHistorical` 필드로 정답/오답 추적 중
- ❌ UI 없음

**충돌 없음**: 데이터 이미 있음, UI만 추가

**예상 공수**: 3일 (plan.md와 일치)
**리스크**: 낮음

---

### 2.6 파일럿 테스트

**충돌 없음**: 코드 작업 아님

**실현가능성 문제**:
- ⚠️ 실제 학교 섭외 필요
- ⚠️ 4주 테스트 기간 확보 어려움
- ⚠️ IRB 승인 필요 가능성

**권장**:
```
1. 소규모 파일럿 (1-2주, 30명)
2. 온라인 테스트 병행
3. 사전-사후 검사는 간소화
```

**예상 공수**: 2-3주 (실제 테스트 시간)
**리스크**: 높음 (외부 협력 필요)

---

## 🔴 Phase 3: 차별화 기능 (8주)

### 실현가능성: 60% ⭐⭐⭐

Phase 3는 **시간이 부족하거나 범위 축소 필요**.

### 3.1 AI 맞춤형 학습

**현재 상태**:
- ✅ `aiAdvisor.ts` 이미 Gemini API 통합
- ✅ 기본 조언 기능 작동 중

**충돌 없음**: 기존 AI 확장

**문제점**:
```typescript
// plan.md의 고도화된 AI 기능은 복잡함
export interface AIAdvisorResponse {
  answer: string;
  personalizedTips: string[];
  recommendedStrategy: {...};
  recommendedResources: [...];
  historicalContext: string;
  modernLesson?: string;
}
```

**현실적 범위 축소**:
```typescript
// 1단계: 기본 맞춤형 조언 (학습 패턴 분석)
// 2단계: 자료 추천
// 3단계: 현대 교훈 (선택)
```

**예상 공수**: 5-7일 (plan.md는 7일)
**리스크**: 중간 (AI 프롬프트 튜닝 필요)

---

### 3.2 빅데이터 분석 시스템

**현재 상태**:
- ❌ 전국 데이터 수집 인프라 없음
- ✅ 방별 데이터는 수집 중

**충돌 발견**: 실현가능성 낮음

**문제점**:
```
1. 중앙 데이터베이스 필요 (현재 인메모리)
2. 많은 사용자 필요 (통계적 의미)
3. 개인정보 보호 문제
```

**권장**: Phase 3에서 제외 또는 단순화
```typescript
// 대안: "이 학급 내 비교" 기능만 구현
// 전국 데이터는 출품 후 확장
```

**예상 공수**: 7일 (but 실용성 낮음)
**리스크**: 매우 높음

---

### 3.3 접근성 기능

**현재 상태**:
- ✅ 반응형 디자인 완성
- ❌ 색약 모드 없음
- ❌ 키보드 단축키 부족

**충돌 없음**: 추가 기능

**예상 공수**: 5일 (plan.md와 일치)
**리스크**: 낮음 (CSS 주로)

---

### 3.4 PWA 오프라인 모드

**현재 상태**:
- ❌ PWA 설정 없음
- ✅ Vite 빌드 시스템 (PWA 플러그인 추가 쉬움)

**충돌 없음**:
```typescript
// vite.config.ts에 플러그인 추가만
import { VitePWA } from 'vite-plugin-pwa';
```

**예상 공수**: 3-4일 (plan.md는 4일)
**리스크**: 낮음

---

### 3.5 성능 최적화

**현재 상태**:
- ✅ 코드 스플리팅 일부 적용
- ⚠️ 번들 사이즈 확인 필요

**충돌 없음**: 최적화 작업

**예상 공수**: 4일 (plan.md와 일치)
**리스크**: 낮음

---

## ⚠️ 충돌 및 문제점 요약

### 1. 타입 정의 중복 가능성

**문제**:
```typescript
// plan.md에서 제안한 타입들이 일부 기존 타입과 겹침

// 기존: types.ts
export interface EventHistory { ... }

// plan.md: 새로 제안
export interface LearningLog { ... }
```

**해결책**:
```typescript
// EventHistory를 확장하거나
// LearningLog로 통합
export interface LearningLog extends EventHistory {
  playerId: string;
  playerName: string;
  logType: 'event_choice' | 'battle' | ...;
}
```

**영향**: 낮음 (타입 정리만 필요)

---

### 2. API 엔드포인트 네이밍

**문제**:
```typescript
// 기존: /api/teacher/dashboard/:roomCode
// plan.md: /api/teacher/dashboard/:roomCode/analytics

// 충돌 없지만 일관성 필요
```

**해결책**:
```typescript
// RESTful 구조로 정리
GET /api/rooms/:roomCode/dashboard
GET /api/rooms/:roomCode/analytics
GET /api/rooms/:roomCode/reports/:playerId
```

**영향**: 낮음 (리팩토링 필요)

---

### 3. 데이터베이스 부재

**문제**:
```typescript
// 현재: 인메모리 저장 (서버 재시작시 데이터 손실)
private rooms: Map<string, GameRoom> = new Map();

// Phase 2부터 영구 저장소 필요
```

**해결책**:
```typescript
// 옵션 1: SQLite (간단, 파일 기반)
npm install better-sqlite3

// 옵션 2: JSON 파일 (가장 간단)
import fs from 'fs';
```

**권장**: SQLite (중간 난이도, 확장 가능)

**영향**: 중간 (Phase 2 초기에 추가 필요)

---

### 4. PDF 생성 라이브러리 선택

**문제**: plan.md는 서버 측 `pdfkit` 제안

**충돌**:
```typescript
// pdfkit: 서버에서만 작동
// 클라이언트에서 미리보기 어려움
```

**해결책**:
```typescript
// 클라이언트 측 PDF 생성 (더 간단)
npm install jspdf html2canvas

// 또는
npm install html2pdf.js
```

**영향**: 낮음 (라이브러리 선택 변경)

---

### 5. 빅데이터 시스템 범위 과다

**문제**: "전국 학생 데이터" 수집은 현실적으로 어려움

**충돌**:
- 출품 시점에 사용자 수 부족
- 중앙 DB 인프라 필요
- 개인정보 보호 이슈

**해결책**:
```typescript
// Phase 3에서 제외 또는 단순화
// "학급 내 통계" 기능으로 축소
```

**영향**: 높음 (Phase 3 범위 조정 필요)

---

## 📊 공수 재평가

### Phase 1 (2주) - 실현가능 ✅

| 작업 | plan.md 예상 | 실제 예상 | 차이 |
|------|-------------|----------|------|
| 학습 데이터 수집 | 3일 | 2-3일 | ✅ 적절 |
| 대시보드 v1 | 3일 | 2-3일 | ✅ 적절 |
| 교육과정 문서 | 1일 | 1일 | ✅ 적절 |
| 수업 지도안 | 2일 | 2일 | ✅ 적절 |
| 매뉴얼 작성 | 3일 | 3일 | ✅ 적절 |
| 피드백 시스템 | 2일 | 2일 | ✅ 적절 |
| **합계** | **14일** | **12-14일** | ✅ **현실적** |

---

### Phase 2 (4주) - 조건부 실현가능 ⚠️

| 작업 | plan.md 예상 | 실제 예상 | 차이 |
|------|-------------|----------|------|
| PDF 리포트 | 5일 | 6-7일 | ⚠️ 약간 빠듯 |
| 역사 자료실 | 5일 | 5-6일 | ✅ 적절 |
| 지도 시각화 | 4일 | 4-5일 | ✅ 적절 |
| 배지 시스템 | 3일 | 3일 | ✅ 적절 |
| 오답 노트 | 3일 | 3일 | ✅ 적절 |
| 파일럿 테스트 | 5일 | 14일 (2주) | 🔴 **과소평가** |
| **합계** | **25일** | **35-36일** | ⚠️ **1주 부족** |

**권장**: Phase 2를 5주로 연장 또는 파일럿 테스트 단순화

---

### Phase 3 (8주) - 범위 축소 필요 🔴

| 작업 | plan.md 예상 | 실제 예상 | 권장 조치 |
|------|-------------|----------|----------|
| AI 맞춤형 학습 | 7일 | 7-10일 | ✅ 유지 |
| 빅데이터 시스템 | 7일 | N/A | 🔴 **제외 권장** |
| 실생활 연계 | 5일 | 3-4일 | ✅ 유지 |
| 접근성 기능 | 5일 | 5일 | ✅ 유지 |
| PWA 오프라인 | 4일 | 3-4일 | ✅ 유지 |
| 성능 최적화 | 4일 | 4-5일 | ✅ 유지 |
| **합계** | **32일** | **22-28일** | ⚠️ **재조정 필요** |

**권장**: 빅데이터 시스템 제외하고 Phase 3를 4-5주로 단축

---

## 🎯 수정된 개발 일정 제안

### 현실적 일정 (총 11주)

```
Phase 1: 출품 필수    ████████████████  2주  (변동 없음)
Phase 2: 경쟁력 강화  ████████████████████████  5주  (+1주)
Phase 3: 차별화      ████████████████  4주  (-4주, 범위 축소)
─────────────────────────────────────────────────────
합계                                    11주
```

### 제외/단순화 권장 항목

**Phase 3에서 제외**:
- 🔴 빅데이터 분석 시스템 (전국 데이터)
  - 대안: 학급 내 비교 기능으로 대체
  - 이유: 인프라 부족, 사용자 수 부족

**Phase 2 단순화**:
- ⚠️ 파일럿 테스트 축소
  - 원안: 90명, 4주, 3개 학급
  - 수정: 30명, 2주, 1개 학급
  - 이유: 섭외 어려움, 시간 부족

**Phase 3 우선순위 조정**:
```
우선순위 1 (필수):
✅ AI 맞춤형 학습
✅ 접근성 기능
✅ 성능 최적화

우선순위 2 (선택):
⚪ PWA 오프라인 (시간 있으면)
⚪ 실생활 연계 (30개 → 10개로 축소)

제외:
🔴 빅데이터 시스템
```

---

## 💡 개선 제안

### 1. 데이터베이스 조기 도입

**현재**: 인메모리 저장
**문제**: Phase 2부터 데이터 영속성 필요
**제안**: Phase 1 Week 2에 SQLite 추가

```typescript
// Week 2 Day 3-4에 추가 작업
npm install better-sqlite3 @types/better-sqlite3

// db.ts 생성
export class Database {
  save(room: GameRoom): void
  load(roomCode: string): GameRoom | null
}
```

**이점**: Phase 2 리포트 생성이 쉬워짐

---

### 2. 콘텐츠 작성 병렬화

**현재 plan.md**: 순차적 작업
**제안**: 콘텐츠 작성은 개발과 병렬 진행

```
개발자 → 코드 작성
교육 전문가 → 동시에 콘텐츠 작성
  - 피드백 메시지 100개
  - 역사 자료 200개
  - 수업 지도안
  - 매뉴얼
```

**이점**: 전체 일정 1-2주 단축 가능

---

### 3. MVP 우선 접근

**제안**: Phase 1-2에 집중, Phase 3는 출품 후

```
출품 필수 (11주):
- Phase 1 완전체
- Phase 2 핵심 기능 (PDF, 자료실, 배지)

출품 후 확장:
- AI 고도화
- 빅데이터 시스템
- PWA
```

**이점**: 출품 마감에 안전하게 대응

---

### 4. 기존 코드 활용 극대화

**제안**: 불필요한 리팩토링 최소화

```typescript
// ❌ 나쁜 예: 전체 재구조화
"기존 코드를 모두 TypeScript strict 모드로 변환"

// ✅ 좋은 예: 점진적 개선
"새 기능만 strict 모드, 기존 코드 유지"
```

**이점**: 개발 시간 절약, 버그 위험 감소

---

### 5. 문서 템플릿 활용

**제안**: 문서 작성에 AI 도구 활용

```
수업 지도안 → Claude/GPT로 초안 생성 후 수정
매뉴얼 → 스크린샷 + AI 설명 자동 생성
역사 자료 → Wikipedia API 활용
```

**이점**: 콘텐츠 작성 시간 50% 단축

---

## 📋 최종 체크리스트

### Phase 1 시작 전 확인 사항

- [ ] SQLite 도입 여부 결정
- [ ] 콘텐츠 작성자 확보 (교육 전문가)
- [ ] 파일럿 테스트 학교 사전 섭외 시작
- [ ] Git 브랜치 전략 수립 (feature/phase1)

### Phase 1 완료 기준

- [ ] 학습 데이터 실시간 수집 확인
- [ ] 대시보드에서 학생별 통계 표시
- [ ] 교육과정 문서 리뷰 완료
- [ ] 수업 지도안 교사 검토 완료
- [ ] 피드백 메시지 최소 50개 작성

### Phase 2 완료 기준

- [ ] PDF 리포트 샘플 생성 확인
- [ ] 역사 자료실 100개 항목
- [ ] 배지 10개 이상 구현
- [ ] 파일럿 테스트 1회 완료
- [ ] 설문조사 응답 30개 이상

### Phase 3 완료 기준

- [ ] AI 맞춤형 조언 3개 패턴
- [ ] 접근성 체크리스트 80% 완료
- [ ] 성능 목표 달성 (로딩 < 3초)
- [ ] 최종 통합 테스트 통과

---

## 🚨 리스크 대응 계획

### Risk 1: 개발 일정 지연 (확률 50%)

**대응**:
1. Phase 3 범위 축소 (빅데이터 제외)
2. Phase 2 파일럿 테스트 단순화
3. 주말 작업 고려

**트리거**: Week 4 진도 60% 미만

---

### Risk 2: 파일럿 테스트 섭외 실패 (확률 30%)

**대응**:
1. 온라인 테스트로 전환 (지인 30명)
2. 가상 데이터로 효과 시뮬레이션
3. 개발 팀 내부 테스트 데이터 활용

**트리거**: Phase 1 완료 시점에 학교 섭외 안 됨

---

### Risk 3: 기술적 문제 (확률 40%)

**대응**:
1. Stack Overflow + Claude 활용
2. 라이브러리 대체 방안 준비
3. 코드 리뷰 주 2회 실시

**주요 리스크 포인트**:
- PDF 생성 (대안: html2pdf.js)
- 지도 시각화 (대안: 정적 이미지)
- AI 프롬프트 (대안: 폴백 메시지)

---

## ✅ 최종 결론

### 전체 평가: ⭐⭐⭐⭐ (실현가능)

1. **Phase 1 (2주)**: ✅ **매우 실현가능** (95%)
   - 기존 코드 기반 탄탄
   - 타입/데이터 구조 대부분 존재
   - 문서 작성이 주 작업

2. **Phase 2 (5주)**: ⚠️ **실현가능** (80%)
   - PDF, 자료실, 배지 구현 가능
   - 파일럿 테스트만 단순화 필요
   - 1주 추가 권장

3. **Phase 3 (4주)**: ⚠️ **조건부** (60%)
   - 빅데이터 시스템 제외 권장
   - AI, 접근성, 성능만 집중
   - 우선순위 명확히 필요

### 권장 최종 일정: **11주** (원안 14주 → 3주 단축)

```
✅ Phase 1: 2주 (변동 없음)
✅ Phase 2: 5주 (+1주, 파일럿 단순화)
✅ Phase 3: 4주 (-4주, 빅데이터 제외)
─────────────────────────────────
   합계:  11주
```

### 성공 가능성 높이는 핵심 전략

1. ✅ **기존 코드 최대 활용** - 이미 13,000줄 완성
2. ✅ **콘텐츠 작성 병렬화** - 개발과 동시 진행
3. ✅ **MVP 우선 접근** - 출품 필수만 집중
4. ✅ **리스크 조기 차단** - 파일럿 사전 섭외, DB 조기 도입
5. ✅ **범위 현실화** - 빅데이터 제외, 파일럿 단순화

### 최종 권고사항

**즉시 시작 가능**: Phase 1은 plan.md 그대로 진행해도 됨
**조정 필요**: Phase 2-3는 이 분석 보고서 반영 권장
**성공 확률**: 조정 후 **85%** (원안 78% → +7%p)

---

**작성자**: 개발팀
**검토 필요**: 교육 전문가, 프로젝트 매니저
**다음 단계**: plan.md 수정 및 팀 회의
